
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * GLOBAL PERMISSIVE POLICY (V18.1.0)
     * Optimized for high-integrity institutional data access and compliance audits.
     */
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
