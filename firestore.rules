rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * GLOBAL PERMISSIVE POLICY (V18.0.0)
     * Simplified for seamless multi-tenant development and data synchronization.
     */
    match /{document=**} {
      allow read, write: if true;
    }
  }
}